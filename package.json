{
  "name": "jobpulse",
  "private": true,
  "workspaces": [
    "shared",
    "bot",
    "monitor",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
