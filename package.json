{
  "name": "jobpulse",
  "private": true,
  "workspaces": [
    "shared",
    "bot",
    "monitor",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "prepare": "prisma generate --schema=shared/src/schemas/schema.prisma",
    "test": "vitest run",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2",
    "vitest": "^3.0.0"
  }
}
